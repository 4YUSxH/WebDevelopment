// We can give custom name to a type

type sankhya = number;
let a: sankhya; // Indirectly it is 'number' type

type value = number | string | boolean;
let b: value // b can be number, string, boolean