import './App.css' // We can import css file 

function App() {
  return <h1 className='We can directly give attributes'>Now we can create html element without using .createelement cause we ars using JSX</h1>
  // Imp : We can only return one element 
  // For returning multiple elements create a parent div and return that parent div 
}

export default App
// All the above code is same as we saw in 2 and 3 folder