let express = require("express") // Kind of importing package

let server = express() // Instance created

server.listen(3000) // Server started, 3000 is port number

// node app.js -> Execute(run) server
// URL -> http://localhost:3000/