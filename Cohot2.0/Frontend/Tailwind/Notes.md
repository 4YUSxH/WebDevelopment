It is a utility-first css framework with classes like felx, pt-4, text-center etc

Tailwind will generate regular .css whenever you type any inline css like flex, text-center, etc 

It follow mobile first approach means reponsive designs

It also enables dark mode feature, use dark: 

In production only used css is shipped and never used css kept unshipped

Read docs for better understanding